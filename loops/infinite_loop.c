#include <stdio.h>

int main() {
  // This is an infinite loop (use with caution!)
  while (1) {
    printf("This loop will run forever!\n");
  }

  return 0; // This line will never be reached due to the infinite loop
}
